
***We all know in React components receive props and state and return HTML. 
When child components receive props from their parents, they either render or pass it to another child.

In the given data, BeneficiaryPayId is parent and parentBenePayId is child.
So we can do it with the HOC as per the output data.
